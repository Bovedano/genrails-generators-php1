meta {
  name: Login
  type: http
  seq: 4
}

post {
  url: {{base_url}}/v1/_auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "jcarracedohz@gmail.com",
    "password": "secret123"
  }
}

script:post-response {
  bru.setVar("token", res.body.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
