meta {
  name: Refresh
  type: http
  seq: 6
}

post {
  url: {{base_url}}/v1/_auth/refresh
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
