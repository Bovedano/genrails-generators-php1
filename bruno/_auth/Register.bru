meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{base_url}}/v1/_auth/register
  body: json
  auth: none
}

body:json {
  {
    "name": "Jorge",
    "email": "jcarracedohz@gmail.com",
    "password": "secret123"
  }
}

script:post-response {
  bru.setVar("token", res.body.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
