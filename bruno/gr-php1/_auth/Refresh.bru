meta {
  name: Refresh
  type: http
  seq: 4
}

post {
  url: {{base_url}}/_auth/refresh
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
